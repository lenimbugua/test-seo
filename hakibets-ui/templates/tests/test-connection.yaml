apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hakibets-ui.fullname" . }}-test-connection"
  labels:
    {{- include "hakibets-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hakibets-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
