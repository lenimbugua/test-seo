apiVersion: v1
kind: Service
metadata:
  name: {{ include "hakibets-ui.fullname" . }}
  labels:
    {{- include "hakibets-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hakibets-ui.selectorLabels" . | nindent 4 }}
